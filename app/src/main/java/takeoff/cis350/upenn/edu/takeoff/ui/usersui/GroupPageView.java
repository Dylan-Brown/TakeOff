package takeoff.cis350.upenn.edu.takeoff.ui.usersui;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;
import android.widget.ListView;

/**
 * Class representing the View to display a group's shared flights
 */
public class GroupPageView extends ListView {

    public GroupPageView(Context context) {
        super(context);
    }

    /**
     * TODO: Display the following inforation
     * Group names need to be displayed
     * Group members on left, title: user's group, group flights on bottom
     * Have to have some kind of method to accept a group invitation
     * Possibly a notification activity that is an intermediary between the authentication and tabs
     * Can do next iteration, now we will just pass the invite as absolute and add them to the group
     * Possibly add names to users so the group functionality is a litle more aesthetic
     */

}
